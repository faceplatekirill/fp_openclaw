Read IMPLEMENTATION/SKILLS_LAYER1/LAYER1-SKILLS-VISION.md
Explore the repository, check what skills are already implemented. 
Then I want you to explore the production project yourself using the same tools as available to the agent.  Find workspace/libs/ecomet-core and use it to explore the live project (don't use the agent). Create at least 5 live test cases for each skill. Then run them against the production live system (read IMPLEMENTATION/SKILLS_LAYER1/OPENCLAW-RUNTIME-VERIFICATION.md). Collect the results. What was the test prompt, what was the expected answer, what was the answer from the agent. If the agent response is wrong or not full, try to tweak the prompt. Iterate up to 5 attempts per test prompt. If the prompt tweaks give the needed effect, try to figure out what should be chaged in the relevant skill docs to make the skill better (use https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices).  Don't change anything in the skills at this stage. Collect your findings into an md file. Don't turn the agents thinking level higher than medium. Don't change the model